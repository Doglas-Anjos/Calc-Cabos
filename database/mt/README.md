# calc_cabos_mt — placeholder

Banco reservado para a futura calculadora de cabos de **média tensão**.
Sem schema ainda — o container `db-mt` (ver `docker-compose.yml`) sobe
vazio, só para a conexão já existir na aplicação e não exigir
reestruturação de infraestrutura quando este módulo for desenvolvido.
